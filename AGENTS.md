# Agent Instructions

## Agent skills

### Issue tracker

工程规格和任务使用 `.scratch/` 下的本地 Markdown。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认的五种 triage 状态。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单领域上下文布局。开始工作前读取根目录 `CONTEXT.md` 和相关 ADR。详见 `docs/agents/domain.md`。
